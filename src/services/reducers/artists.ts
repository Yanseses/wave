
const artistsState = {
  topChart: null,
  currentArtist: null
}

export const artistsReducer = (state = artistsState, action: any) => {
  switch(action.type){
    default: {
      return state
    }
  }
}